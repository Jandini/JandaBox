{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_CommandBox",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
//#if (path == "")
                "${workspaceFolder}/CommandBox.csproj"
//#else
                "${workspaceFolder}/VS_PATH/CommandBox/CommandBox.csproj"
//#endif
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}